#include <stdio.h>
#include <stdbool.h>
#include <stdlib.h>

int nose(int a) {
    return 
}

int main(int argc, char*argv[]) {
 int a = atoi(argv[1]);
    int b = atoi(argv[2]);
    bool same = a == b;
    printf(%sn, same ? 1 : 0);
    return 0;
}
